# Checkpoint — 2026-03-27 00:35

**Progetto:** super-bot
**Branch:** main

## Task in corso
- Implementazione range check ingresso per segnali Svetli su Autotrade V2 (produzione)

## Completato
- Analisi storica: 3 segnali con range su 71 totali, simulato impatto (SIG#81 sarebbe stato bloccato)
- `signal_parser.py`: aggiunti campi `entry_low` e `entry_high` al dataclass Signal, parser preserva estremi range
- `brokers/capital.py`: nuovo metodo `get_current_price(symbol)` via API Capital.com `/api/v1/markets/{epic}`
- `router.py`: nuovo gate `_range_check()` — confronta prezzo corrente con range, blocca + alert se fuori
- Test unitari e simulazione storica superati
- Autotrade riavviato in tmux `trader` su ollasrv (kill vecchio processo su :8080, nuovo attivo)
- Copie locali dei file remoti in `tools/` (signal_parser_remote.py, router_remote.py, capital_remote.py)
- CHANGELOG e MEMORY aggiornati

## Modifiche in corso
- `tools/signal_parser_remote.py` — copia locale del parser con range support
- `tools/router_remote.py` — copia locale del router con range check gate
- `tools/capital_remote.py` — copia locale del broker con get_current_price()
- `memory/CHANGELOG.md` — aggiunta sessione 2026-03-27

## Decisioni prese
- Range check nel router (non nel broker): e' una decisione di routing, non di esecuzione
- Per BUY si usa offer, per SELL si usa bid come prezzo di confronto
- Se impossibile ottenere il prezzo, il trade passa (fail-open per non bloccare segnali validi)
- entry_price resta la media per backward compatibility con DB e display

## Prossimi passi
- Commit + push su GitHub

## Blocchi / Note
- Errore Binance API key preesistente (non correlato a questa modifica)
- La sessione tmux precedente ("openclaw") non esisteva piu, ricreata come "trader"
